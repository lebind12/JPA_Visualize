spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        default_batch_fetch_size: 100
logging:
  level:
    org.hibernate.SQL: WARN
