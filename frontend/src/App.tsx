import { Button } from '@/components/ui/button'

export default function App() {
  return (
    <div className="min-h-svh flex flex-col items-center justify-center gap-6 bg-background text-foreground">
      <h1 className="text-4xl font-semibold tracking-tight">JPA Lab</h1>
      <p className="text-muted-foreground">Phase 1 scaffolding — Vite + React + Tailwind + shadcn/ui 정상 동작</p>
      <Button>Placeholder</Button>
    </div>
  )
}
